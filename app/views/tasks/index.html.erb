<h2>TASK LIST</h2>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <!-- !-->
      <%# if @task.completed == true %>
      <!-- <i class="fa fa-check-square-o" aria-hidden="true"></i> -->
      <%# elsif %>
      <!-- <i class="fa fa-square-o" aria-hidden="true"></i> -->
      <%# end %>
      <!--  -->
      <%= link_to task.title, task_path(task) %><%= link_to "  🗑", delete_task_path(task), method: :delete, data: {confirm: 'Are you sure you want to delete this task permanently?'} %></li>
  <% end %>
</ul>

<%= link_to "➕ Add new task", new_task_path %>

